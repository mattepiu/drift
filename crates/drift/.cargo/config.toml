[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = ["-C", "link-arg=-fuse-ld=mold"]

[target.aarch64-unknown-linux-gnu]
linker = "clang"
rustflags = ["-C", "link-arg=-fuse-ld=mold"]

# macOS and Windows use default linker — no overrides needed.

[env]
RUST_BACKTRACE = "1"

[build]
# Incremental compilation for dev builds
incremental = true
